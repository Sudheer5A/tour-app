import { StyleSheet, Text, View } from "react-native";
import React from "react";

const PlaceScreen = ({ name }) => {
  return (
    <View>
      <Text>{name}</Text>
    </View>
  );
};

export default PlaceScreen;

const styles = StyleSheet.create({});
